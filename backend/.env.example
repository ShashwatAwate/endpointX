# Server port
PORT=3000

# JWT secret used to sign tokens — replace with a strong random value in production
JWT_SECRET=replace_with_a_secure_random_string

# PostgreSQL connection string (optional). Example:
DATABASE_URL=postgresql://neondb_owner:npg_HhQO7U0cmRKE@ep-curly-smoke-a135j506-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Alternatively, provide individual PG_* vars used by pg:
# PGHOST=localhost
# PGUSER=postgres
# PGPASSWORD=yourpassword
# PGDATABASE=your_db
# PGPORT=5432

# Node environment
NODE_ENV=development
